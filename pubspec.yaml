
name: flutter_navx
description: A lightweight and highly customizable bottom navigation bar component for Flutter.
version: 0.0.1
homepage: https://github.com/superxabc/flutter_navx

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

